 dataSource() {
    return issueDispositionDetails.map(detail => ({
      asofDate: detail.asofDate,
      processDate: detail.processDate,
      errorCount: detail.errorCount,
      errorBalance: detail.errorBalance
    }));
  }
