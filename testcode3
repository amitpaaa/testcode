openDialog(): void {
    this.issueDetailService.getIssueDetails().subscribe((issueDetailForm) => {
        this.dialog.open(DailyIssueDetailsComponent, {
            data: { issueDetailForm },
            height: '90%',
            width: '50%',
        });
    });
}
