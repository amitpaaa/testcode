 loadMockData() {
    // Mock data is already set as this.dataSource
    this.loadUniqueKeyData(this.dataSource.data[0]);
  }

  loadUniqueKeyData(rowData): void {
    this.uniqueKeyDataSource = {
      data: rowData.uniqueKeyData,
      totalRecords: rowData.uniqueKeyData.length,
      currentPage: 1,
      totalPages: 1,
      message: 'Success'
    };
  }
