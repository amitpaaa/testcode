loadMockData() {
    // Mock data is already set as this.dataSource
    this.loadUniqueKeyData(this.dataSource.data[0].uniqueKeyData);
  }

  loadUniqueKeyData(uniqueKeyData): void {
    this.uniqueKeyDataSource = {
      data: uniqueKeyData,
      totalRecords: uniqueKeyData.length,
      currentPage: 1,
      totalPages: 1,
      message: 'Success'
    };
  }
