cellRenderer: (params) => {
            if (params.value) {
                const date = new Date(params.value);
                return date.toLocaleDateString(); // Formats the date according to the locale, without time
            }
            return '';
        }
