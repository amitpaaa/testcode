getCheckDetails(payload: SearchRequest): Observable<PagingResponse<CheckDetails>> {
    // Use mock data for now
    return of(CheckDetailsResponse).pipe(
      catchError((err) => {
        throw err;
      })
    );
